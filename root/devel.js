define([
    'amber/devel',
    'deploy',
    // --- packages to be deployed begin here ---
    '{%= namespace %}/{%= name %}-Tests'
    // --- packages to be deployed end here ---
], function (smalltalk) {
    return smalltalk;
});
